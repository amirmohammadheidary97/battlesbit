import { Skeleton } from "@mui/material";

const FullPageSkeleton = () => (
    <Skeleton variant="rectangular" animation="pulse" width={'100vw'} height={'100vh'} />
);

export default FullPageSkeleton;
